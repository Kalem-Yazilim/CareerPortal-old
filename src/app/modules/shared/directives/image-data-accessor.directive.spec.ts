import { ImageDataAccessorDirective } from './image-data-accessor.directive';

describe('ImageDataAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDataAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
